# intent-flow-cli
IntentFlow CLI is a rules-based NLP tool for Node.js. Instead of using machine learning, it uses a transparent scoring system of regex and keywords to reliably match text to intents. It's designed for speed and predictability in chatbots, CLI tools, and routing systems.
